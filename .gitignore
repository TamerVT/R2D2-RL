# Local-only / build artifacts
__pycache__/
*.pyc
*.pyo
*.egg-info/
*.so

# MuJoCo runtime log
MUJOCO_LOG.TXT

# Internal-only docs / spec / assignment PDF (not for the public repo)
AGENTS.md
hybrid_control_rl_trajectory_spec.md
*.pdf
*.pdf:Zone.Identifier

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Parked LeRobot-pipeline plan (kept locally, not shipped)
legacy/

# External repositories cloned for local audit/integration planning
external/

# RL logs / checkpoints (created later)
logs/
runs/
wandb/
checkpoints/

# Skip generated artifact subfolders we don't curate; the curated ones
# below are committed because they document the pipeline output.
# Outputs live under OUR_stuff/outputs/ after the 2026-05-14 restructure.
OUR_stuff/outputs/*
!OUR_stuff/outputs/wrist_cam_demo/
!OUR_stuff/outputs/left_wrist_demo/
!OUR_stuff/outputs/project3_screenshot/
!OUR_stuff/outputs/pixel_to_table_validation/

# Old top-level outputs/ from before the restructure
outputs/
